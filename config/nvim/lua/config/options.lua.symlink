-- vi: ft=lua

local opt = vim.opt
local globals = vim.g

-- opt.winbar = "%=%m %f"

opt.listchars = {
	-- space = "⋅",
	-- nbsp = "_",
	tab = "__",
	eol = "↴",
	trail = "•",
	extends = "❯",
	precedes = "❮",
}

opt.clipboard = "unnamed"

globals.root_spec = { "cwd" }
